syntax on
set smartindent
set number

set tabstop=4
set shiftwidth=4
let _curfile = expand("%:t")
if _curfile =~ "Makefile" || _curfile =~ "makefile" || _curfile =~ ".*\.mk"  || _curfile =~ "rules"
    set noexpandtab
else
    set expandtab
endif

set pastetoggle=<F10>

